import React from 'react';
import { View, Text } from 'react-native';

const Add: React.FC = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        width: '100%',
      }}>
      <Text>Erro!</Text>
    </View>
  );
};

export default Add;
